<header class="flex h-16 items-center justify-between border-b border-base-300 bg-base-100 px-4 lg:px-6">
  <div class="flex items-center gap-2 sm:gap-3">
    <button class="btn btn-ghost btn-sm lg:hidden" aria-label="<%= mobile_menu_aria_label %>">
      <span class="size-6"><%= helpers.heroicon "bars-3" %></span>
    </button>

    <%= greeting %>
  </div>

  <%= user_dropdown %>
</header>
