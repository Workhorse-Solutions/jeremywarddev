<%= turbo_stream.replace "modal" do %>
  <%= turbo_frame_tag "modal" %>
<% end %>

<%= turbo_stream.prepend "flash" do %>
  <%= render UI::ToastsComponent.new(flash: { "notice" => t("authenticated.email_changes.update.notice") }) %>
<% end %>
